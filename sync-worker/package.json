{
  "name": "berkdoc-sync-worker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Cloudflare Worker + Durable Object hosting tldraw realtime board sync (the cloud counterpart of the local NestJS :3001 sync gateway).",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@tldraw/sync-core": "4.2.3",
    "@tldraw/store": "4.2.3",
    "@tldraw/tlschema": "4.2.3",
    "@tldraw/validate": "4.2.3",
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.6.2",
    "wrangler": "^3.78.0"
  }
}
